# Сканер папок — дизайн и логика

> Файл-память: тут зафиксирован актуальный дизайн и логика приложения.

## Стек
- Backend: Rust + Tauri 2 (`src-tauri/`)
- Frontend: TypeScript + Vite + vanilla DOM (без фреймворков), Chart.js (`src/`)
- Модули Rust: `scan.rs` (классический обход), `mft.rs` (быстрый путь через MFT, Windows), `dup.rs` (дубликаты), `hash.rs` (blake3), `disk.rs` (использование диска), `reveal.rs` (показ в проводнике)

## Окно и внешний вид
- Фреймлесс окно, размер **1450×800**
- **Чистый чёрный фон** (#000000), шрифт **Inter**
- Табы: «Обзор» | «Дубликаты файлов» — стиль **пилюли** (pill-chip), без подчёркивания
- Кнопка «Сканировать новую папку» — синяя градиентная, справа в шапке
- Заголовок «Сканер папок» — жирный, белый, 20px

## Экраны

### Начальный экран (до сканирования)
- По центру: заголовок «Введите путь к папке для сканирования»
- Подзаголовок «Или откройте папку через кнопку» (серый, курсив)
- Инпут для пути (тёмно-серый, скруглённый 12px)
- Кнопка «Выбрать папку» (тёмно-серая)
- Кнопка «Сканировать папку» (синяя градиентная)

### Сканирование
- Подзаголовок «Обзор папки "путь"»
- Прогресс-бар (синий на тёмном фоне)
- Текст «Сканирую папку...»

### Результаты обзора
- Заголовок «Обзор папки "путь"»
- Две колонки:
  - **Левая**: две карточки «Диаграмма папок» и «Диаграмма расширений» (тёмно-серые, #1a1a1e, скруглённые 12px)
  - **Правая**: карточка «Результаты сканирования» (папки/файлы/размер/время) + карточка «Топ-10 больших файлов»

### Дубликаты
- Заголовок «Дубликаты в папке "путь"»
- Информационный текст (серый)
- Кнопка «Начать поиск дубликатов» (синяя)
- Таблица с колонками: Файл | Размер | Путь
- Группы: заголовок «N копии - имя файла» (синеватый фон)
- Итог внизу: «Всего групп: N - Можно освободить: Xмб»

## Диаграммы
- Chart.js Doughnut (pie), cutout 0% (сплошной круг)
- Палитра: #FF6B35, #4CAF50, #2196F3, #FFC107, #9C27B0, #E91E63, #00BCD4, #8BC34A
- Легенда справа с точками (circle), белый текст
- Секторы <2% (кроме 3 крупнейших) сливаются в «Остальное», максимум 8 секторов

## Логика скана
- Сканируются: файлы, папки, размеры, расширения, топ-10 файлов, кандидаты дубликатов (>= 1 МБ)
- Системные папки (Windows, System32, Program Files, ProgramData) ЧИТАЮТСЯ в статистику. Исключаются только служебные/кэш-папки по ИМЕНИ: WinSxS, AppData, Installer, Temp, $Recycle.Bin, System Volume Information, node_modules, .git и т.п.
- Сборка результата: `ScanResult` = total_size, total_files, total_folders, folder_sizes, extension_sizes, top_files, dup_candidates, system_skipped
- Прогресс: событие `scan-progress` (u8 0-100, монотонный), завершение — `scan-done`

## Дубликаты
- Группировка: (имя файла, размер), затем хеш blake3 выборочных 1 МБ (начало/середина/конец)
- Кандидаты: только файлы >= 1 МБ
- Результат: `find_duplicates` -> события `dup-progress`, группировка по статусу обработки

## Read-only
- Приложение ПОЛНОСТЬЮ read-only: удаление, отмена — ВСЁ УДАЛЕНО из кода
- Поиск дубликатов только ПОКАЗЫВАЕТ файлы (двойной клик — открыть в проводнике)

## Производительность
- Windows: `DirEntry::metadata()` БЕСПЛАТЕН (данные из FindFirstFile)
- Скан = std::fs::read_dir + очередь папок, воркеры берут из очереди (`QueueCtx`)
- **Агрегация без глобальных мьютексов**: каждый воркер копит в ЛОКАЛЬНЫЙ `AggInner`
- **Прогресс монотонный**: % = finished/spawned, emit через `compare_exchange`, +3%/25ms
- Бенч (релиз): C:\Windows = 138.7k файлов/41.8k папок/13.8 ГБ за ~4.0s; C: = 344.5k файлов/67.8k папок/80.6 ГБ за ~5.6s тёплый

## Быстрый путь: MFT (модуль `mft.rs`, только Windows)
- Windows-команда `start_scan` СНАЧАЛА пробует MFT: `mft::scan_mft`; при `None` — автоматически классический `scan_root`.
- Алгоритм: `Volume::new(r"\\.\C:")` -> `Mft::new` читает весь `$MFT`+`$Bitmap` в RAM -> за один проход извлекаются (parent, имя, размер, is_dir) -> индекс parent→children -> DFS по поддереву с агрегацией
- MFT сканирует ВЕСЬ том всегда (даже для маленькой папки)
- Бенч (нужен запуск от админа): `BENCH_MFT=<path> cargo test --release --lib bench_mft -- --ignored --nocapture`

## UI (последние правки)
- Начальный экран: по центру с инпутом и кнопками
- Кнопка «Сканировать новую папку» в шапке — сбрасывает на начальный экран
- Табы переключаются во время сканирования (UI не блокируется)
- Прогресс только один: полоса на «Обзоре»
- Дубликаты: таблица с группами, итог внизу
- Кнопка «Найти дубликаты» снова кликабельна после каждого прогона

## Окружение / сборка
- Node portable: `.tools/node-v24.21.0-win-x64` (подключить в PATH)
- `npm.cmd`/`tauri.cmd`/`tsc.cmd`, никогда `.ps1`
- Release: `cargo build --release` в `src-tauri/`
- Бенчмарки: `BENCH_PATH=<path> cargo test --release --lib bench_scan_release -- --ignored --nocapture`
- Не использовать `panic = "abort"` (tauri/rayon требуют unwinding)

## Админ-права (Windows)
- Манифест НЕ переопределяем
- Запуск — обычным пользователем, БЕЗ UAC, НИКАКИХ кнопок/баннеров про админа в UI
- MFT-путь сам проверяет права и при их отсутствии автоматически делает обычный walk
